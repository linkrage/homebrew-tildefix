# homebrew-tildefix

Homebrew tap for [TildeFix](https://github.com/linkrage/TildeFix) — fix the `§` tilde/backtick key on UK/ISO Mac keyboards.

## Install

```bash
brew tap linkrage/tildefix
brew install --cask tildefix
```

## Uninstall

```bash
brew uninstall --cask tildefix
```
